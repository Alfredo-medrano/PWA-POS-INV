
  # PWA Sistema POS e Inventario

  This is a code bundle for PWA Sistema POS e Inventario. The original project is available at https://www.figma.com/design/seaNWvZmvcq50mrKGQx5nc/PWA-Sistema-POS-e-Inventario.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  